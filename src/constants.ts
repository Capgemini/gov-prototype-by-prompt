// On the history tab we show the last 7 prototypes, plus the current one
export const NUMBER_OF_PREVIOUS_PROTOTYPES_TO_SHOW = 7;

// For pagination, these constants define the options for how many items to show per page
export const PER_PAGE_OPTIONS = [5, 10, 20, 50];
export const DEFAULT_PER_PAGE = 10;
